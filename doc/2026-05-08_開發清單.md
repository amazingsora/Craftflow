# Craftflow 開發清單

**建立日期：2026-05-08**
**當前版本：product_plan.md v0.2**

---

## 🔴 明天優先（Phase 1 收尾）

### C. 端到端測試（Swagger UI）

- [ ] 建立測試專案 `POST /api/v1/projects/`
- [ ] 新增一個有段落內容的章節 `POST /api/v1/projects/{id}/chapters`
- [ ] 新增一個角色 `POST /api/v1/projects/{id}/characters`
- [ ] 跑章節節奏分析 `POST /api/v1/chapters/{id}/analyze` → 確認 rhythm 資料回傳正常
- [ ] 跑角色設計問答 `POST /api/v1/characters/{id}/ask` → 確認 llama3 文字回應正常
- [ ] 上傳一張插畫 `POST /api/v1/projects/{id}/illustrations`
- [ ] 跑圖片描述 `POST /api/v1/illustrations/{id}/describe`

### 修正項目（測試時同步處理）

- [ ] 拉視覺模型（擇一）：
  - `ollama pull llava`（約 4GB，快速測試用）
  - `ollama pull qwen2-vl`（約 8GB，**推薦**，中文理解較好）
- [ ] 確認 `backend/app/services/ai/ollama_client.py` 的 `DEFAULT_TEXT_MODEL` 和 `DEFAULT_VISION_MODEL` 與實際安裝模型一致
- [ ] 驗證視覺端點正常：`POST /api/v1/illustrations/{id}/analyze`（mode: sketch_critique）
- [ ] 驗證角色肖像描述：`POST /api/v1/characters/{id}/describe-portrait`

---

## 🟡 Phase 2 — Web UI（React）

### 基礎建設

- [ ] `frontend/` 目錄初始化（React + Vite）
- [ ] API client 封裝（axios，base URL 指向 `http://127.0.0.1:8000`）
- [ ] 全域路由設計（react-router）
- [ ] 基本 Layout（側邊欄導覽、主內容區）

### 頁面開發（建議順序）

- [ ] 專案列表頁（建立、選擇、刪除專案）
- [ ] 章節管理頁（新增章節、拖曳排序、刪除）
- [ ] 章節編輯器（文字輸入框、自動儲存、字數統計）
- [ ] 人物設定頁（新增角色、填寫各欄位、AI 問答入口）
- [ ] 插畫管理頁（上傳圖片、掛載到章節或角色）
- [ ] AI 分析結果頁（節奏色塊視覺化、一致性報告、改寫建議並排對比）
- [ ] 角色設計問答介面（對話式 Q&A，顯示現有設定為側邊 context）
- [ ] 繪圖輔助問答介面（可選附圖、freeform 問答）
- [ ] 系統狀態列（顯示 Ollama / ComfyUI 在線狀態）

---

## 🟢 Phase 3 — Flutter App

- [ ] Flutter 專案初始化（iOS + Android）
- [ ] 串接後端 API
- [ ] 核心頁面移植（章節、角色、插畫、AI 問答）
- [ ] 手機相機直接匯入插畫
- [ ] 離線草稿模式（本地暫存）

---

## ⚪ Phase 4 — 雲端與產品化（未來）

- [ ] 小說匯出 API（Markdown 含目錄，Phase 1 遺留）
- [ ] PostgreSQL 遷移
- [ ] 用戶帳號系統
- [ ] Claude API / OpenAI 作為 AI provider 選項（可切換）
- [ ] 雲端圖片儲存（S3-compatible）
- [ ] EPUB / PDF 匯出

---

## 備註

- 目前 Ollama 已安裝：`llama3:latest`（純文字，**無視覺能力**）
- 視覺功能需另行安裝 `llava` 或 `qwen2-vl`
- ComfyUI 已確認在線（風格強化 / 線稿化功能可用）
- 後端 API 文件：`http://127.0.0.1:8000/docs`
- 規劃文件：`doc/product_plan.md`
