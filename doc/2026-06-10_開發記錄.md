# 2026-06-10 開發記錄

承接 2026-06-09

---

## 主題：生圖加速 P6-1 / P6-2（VRAM 條件式卸載 + vision 快取）

對應規劃：`doc/2026-06-10_功能改進規劃.md` P6

---

## 今日修改

### 1. VRAM Guardian 條件式卸載（P6-1）

**根因**：`request_focus` 原為非黑即白——切到 Ollama 就叫 ComfyUI `/free` 全卸載，切回來 SDXL checkpoint 整顆重載（10~30s）。角色生圖流程「vision → compile（Ollama）→ 生圖（ComfyUI）」每次都付一次重載。

**變更**：
- `core/config.py`：新增 `VRAM_COEXIST_ENABLED`（預設 true，可 env 關閉退回舊行為）、`COMFYUI_REQUIRED_VRAM_GB=8`、`OLLAMA_REQUIRED_VRAM_GB=7`
- `services/ai/vram_manager.py`：新增 `_can_coexist()`——切換前查 ComfyUI `/system_stats`（GPU free / torch reserved）與 Ollama `/api/ps`（size_vram），放得下就只轉移 focus 不卸載；**任何查詢失敗一律回 False 退回舊卸載路徑**（resilient errors）

### 2. vision 抽取結果快取（P6-2）

**根因**：concept images 不變，vision 抽取結果就不變，但每次生成都重打 Ollama vision（5~20s）。

**變更**（`api/art_generate.py`）：
- 新增 `_vision_extract_cached()`：以 image bytes SHA-256 + 模式（coverage/plain N）+ vision model 為 key，in-memory 快取（上限 32 筆，FIFO 淘汰）；錯誤結果（`[...]` 開頭）不快取；cache hit 連 `request_focus("ollama")` 都跳過，ComfyUI 保持 warm
- `generate_character_design` 與 `generate_variant_design` 兩端點改用同一 helper（順帶去掉一段重複碼）

### 3. 修復：檔案掛載同步截斷

編輯過程中 `art_generate.py` 與 `vram_manager.py` 在 sandbox 掛載側發生尾端截斷，已從 git HEAD 補回缺失尾段並全檔重寫修復，`py_compile` 三檔通過，diff 確認僅含本日預期變更。

---

## 驗證

- `py_compile`：art_generate.py / vram_manager.py / config.py ✅
- 快取 key/淘汰邏輯獨立測試 ✅（同圖 hit、異圖/異模式 miss、FIFO 淘汰）
- `git diff` hunks 核對：僅含預期變更 ✅

## 待驗收（實機）

- [ ] 16GB 卡上共存模式：連續生成兩次同角色，確認第二次無 checkpoint 重載、無 vision 呼叫（看 log `[vision-cache] hit` 與 `VRAM: enough memory`）
- [ ] VRAM 不足情境（大 checkpoint）：確認自動退回卸載路徑、不 OOM
- [ ] 更換 concept image 後快取正確失效（hash 改變）

## 技術備忘

- 快取為 in-memory，後端重啟即清空；若日後做 generation_history（P3）可考慮落 DB
- `_COMFYUI_RESIDENT_BYTES = 4GiB`：torch reserved 超過此值視為 checkpoint 駐留
- 變體端點原本就沒有 pixel coverage fallback——維持原狀未補（surgical update）

---

## 主題二：P1 資料安全（章節版本快照 + DB 備份）

### 4. chapter_revisions 章節版本快照（P1-1）

**根因**：Chapter 僅單一 content 欄位，覆寫即遺失；product_plan「創作歷程可回溯」未實作。

**變更**：
- `models/chapter_revision.py`（新）：ChapterRevision（chapter_id FK CASCADE、title、content、created_at、content_length property）
- `models/chapter.py`：加 `revisions` relationship（cascade all, delete-orphan）
- `api/chapters.py`：`_snapshot_chapter()` — PUT 時內容有變才快照舊版；空內容不快照；每章保留 `CHAPTER_REVISIONS_KEEP`（預設 20）版，FIFO 淘汰
- 新表由 `create_all` 自動建立，無需手寫 migration

### 5. 版本歷史 API（P1-2）

```
GET  /chapters/{id}/revisions                     ← 列表（含字數，不含全文）
GET  /chapters/{id}/revisions/{rev_id}            ← 單版全文
POST /chapters/{id}/revisions/{rev_id}/restore    ← 還原（還原前先快照當前內容）
```

### 6. SQLite 定時備份（P1-3）

- `core/backup.py`（新）：`VACUUM INTO` 產生一致性快照至 `data/backups/`，保留 `BACKUP_KEEP`（預設 7）份
- `main.py` lifespan：啟動備份一次 + 每 `BACKUP_INTERVAL_HOURS`（預設 24，0=停用）背景執行
- 備份失敗只記 log 不影響運作（resilient errors）

### 設定新增（`core/config.py`，皆可 env 覆寫）

`CHAPTER_REVISIONS_KEEP=20`、`BACKUP_DIR=data/backups`、`BACKUP_KEEP=7`、`BACKUP_INTERVAL_HOURS=24`

## 驗證（P1）

- in-memory DB + TestClient 整合測試 7 項全過：快照觸發/不觸發（標題、同內容）、KEEP 淘汰、還原（含還原前快照）、跨章節 404 防護、cascade 刪除、備份產生與淘汰、備份檔可開啟含資料表
- 全後端 `py_compile` 通過

## 待辦

- [ ] 前端版本歷史 UI（NovelTab 編輯器加「歷史版本」入口）— UI 由使用者方處理中
- [ ] 實機驗收：啟動後確認 `data/backups/` 產生備份、儲存章節後 revisions API 有資料

## 技術備忘（P1）

- 編輯期間檔案掛載同步多次發生尾端截斷（chapters.py、chapter.py、schemas/chapter.py、models/__init__.py），均已重寫修復並全量 py_compile 核對——**commit 前建議再跑一次後端啟動煙霧測試**
- restore 時若 revision 內容與當前相同則不動作（冪等）

---

## 主題三：P2 Markdown 匯出

### 7. 匯出服務 + API（P2-1）

**動機**：product_plan 2.4 完全未實作，小說寫完出不去——價值閉環最後一哩。

**變更**：
- `services/export_service.py`（新）：組書稿 Markdown（書名頁 → 目錄 → 未分卷章節 → 各卷章節），插圖依 `linked_chapter_id` 附於章節末（`![caption](images/...)` + 引用說明）
- `api/export.py`（新）+ `main.py` 掛 router

```
GET /projects/{id}/export/markdown             ← zip：{書名}.md + images/（預設，可攜帶完整書稿）
GET /projects/{id}/export/markdown?format=md   ← 純 Markdown 文字
```

**設計決策**：
- 未分卷章節（volume_id NULL，建卷功能前的舊資料）排在各卷之前
- 卷標題「第N集」用中文數字，副標題以全形空格相接
- 遺失的插圖檔記 log 略過，不中斷匯出（resilient errors）
- 中文檔名下載用 RFC 5987 `filename*` 編碼

## 驗證（P2）

- TestClient 整合測試通過：md 結構（書名/作者/簡介/目錄/卷章順序/插圖引用）、未分卷在前、zip 內容與 md 一致、遺失圖檔略過、404
- 全後端 py_compile 通過

## 待辦（P2 後續）

- [ ] EPUB / PDF 匯出（v2）
- [ ] 角色設定集匯出 PDF
- [ ] 前端匯出按鈕（NovelTab）— UI 範疇

---

## 主題四：P3 generation_history（生成可重現）

### 8. 模型 + 記錄（P3-1）

**根因**：seed 只在 response header，無法回溯重現舊圖。

**變更**：
- `models/generation_history.py`（新）：endpoint / character_id / variant_slot / seed / workflow / style / positive / negative / params(JSON) / created_at。character_id 為純 Integer 非 FK——歷史是日誌，角色刪除後記錄保留
- `services/ai/generation_recorder.py`（新）：`record_generation()` 永不 raise，記錄失敗只記 log（resilient errors）
- `api/art_generate.py`：三個端點生成成功後記錄——`/art/generate`、`generate_character_design`、`generate_variant_design`；params 含 steps/尺寸/IPA/CN 權重/coverage/loras/timings 等
- Response 新增 `X-History-Id` header，前端存圖時可關聯

### 9. 查詢 API（P3-2）

```
GET    /generation-history?character_id=&endpoint=&limit=   ← 列表（brief，由新到舊）
GET    /generation-history/{id}                              ← 詳情（含 prompt + params，供重跑）
DELETE /generation-history/{id}
```

## 驗證（P3）

- 整合測試 5 組全過：record helper（三端點型態）、壞 session 不 raise、列表過濾（character/endpoint 組合）、brief 不含全文、詳情 params、404、刪除
- 全後端 py_compile 通過

## 待辦（P3 後續）

- [ ] 前端「還原參數重跑」入口（讀 X-History-Id / 歷史列表）— UI 範疇
- [ ] compose / img-guide / ipadapter 端點補記錄（次要流程）

## 技術備忘（P3）

- 新表由 create_all 自動建立
- art_generate.py 改以 Python 字串替換 patch（避免 Edit 工具掛載截斷問題），anchor 唯一性以 assert 確認

---

## 主題五：P4 生圖非同步 job + P5 批次生成

### 10. 非同步 job（P4）

**根因**：`/art/generate` 同步等待回傳 bytes，two-pass / 高解析度 / 批次會撞 HTTP 逾時。

**變更**：
- `services/ai/generation_jobs.py`（新）：in-memory job store（單人本地工具不落 DB），上限 30 筆、完成後保留 30 分鐘；`run_txt2img_job()` 錯誤一律收進 job.error 不外拋；history 記錄用獨立 session
- `api/art_generate.py`：
  - 抽出 `_build_txt2img()`（sync 與 async 共用 workflow 組裝——順帶解決一處重複）
  - 原 `/art/generate` 行為不變（向下相容）

```
POST /art/generate-async                ← 立即回傳 {job_id, seed, batch_size}
GET  /art/jobs/{job_id}                 ← 狀態（queued/running/done/error + elapsed + meta）
GET  /art/jobs/{job_id}/result?index=N  ← 取第 N 張 PNG（X-Seed / X-History-Id headers）
```

### 11. 批次生成（P5 部分）

- `GenerateAsyncRequest.batch_size`（1~8，clamp）→ `EmptyLatentImage.batch_size`
- 同 seed 一次出多張（batch index 變化），一次 checkpoint 載入攤平成本，挑圖效率倍增
- generation_history 記錄含 batch_size

### 12. 修復：config.py 同步截斷

config.py 在掛載側被截斷（VRAM/Personal Style 區塊遺失），已完整重寫並驗證所有設定值。

## 驗證（P4/P5）

- job service 測試（mock ComfyUI）：批次 4 張、history 落 DB（params 含 batch_size）、ComfyUI 失敗 → job error 不外拋、store 上限淘汰
- API 測試：404 / 409（未完成）/ 500（error job）/ index 越界、X-Batch-Index header
- **啟動煙霧測試：`from main import app` 成功，126 routes，今日全部新端點（async job / history / revisions / export）均已掛載**

## 待辦（P5 剩餘）

- [ ] inpainting（局部重繪）工作流
- [ ] upscale / 高解析度修復工作流
- [ ] 前端批次挑圖 UI — UI 範疇

## 技術備忘（P4/P5）

- job store 為 in-memory，後端重啟即清空（結果 bytes 不適合落 DB；確認需求後可改存檔）
- ComfyUI job timeout 放寬至 600s（批次/高解析度）
- 同 seed 批次：重現一整批 = 同 seed + 同 batch_size
