# 2026-06-10 優化規劃

來源：Fable 5 專案分析（優缺點盤點）。全方向納入，依「還債 → 體驗 → 新功能 → 收尾」排序。

## A. 技術債（優先）

- 🔲 A1. 拆解 `art_generate.py`（現 3,075 行）— 業務邏輯下沉 services，API 層只留路由（詳細規劃見 `doc/2026-06-12_A1拆解規劃.md`，待其他分支整理後動工）
- 🔲 A2. 拆解 `CharacterTab.jsx`（2,431 行）— 按功能區塊拆子元件
- 🔲 A3. 測試基礎建設 — pytest + prompt_engine / consistency 單元測試 + API smoke test
- 🔲 A4. 狀態持久化 — `core/state.py` 全域變數改存 DB／設定檔（重啟不遺失）
- 🔲 A5. Alembic migration — schema 版本化

## B. 體驗

- 🔲 B1. 任務佇列 + 即時進度 — 生圖／訓練改 job queue + SSE/WebSocket（取代輪詢）
- 🔲 B2. 備份機制 — SQLite + uploads 一鍵備份／還原

## C. 新功能

- 🔲 C1. 小說匯出 — product plan 2.4（EPUB / PDF / TXT）
- 🔲 C2. 本地語意搜尋（RAG）— 章節／角色設定本地 embedding 檢索，輔助一致性分析

## D. 收尾既定目標

- 🔲 D1. legacy CLI 遷移 — `tools/Craftflow` → `services/ai/`
- 🔲 D2. 雙主題逐 Tab 目視驗證（承 06-10 待辦，可併 A2 處理）
- 🔲 D3. 側欄響應式收合（承 06-10 待辦）
- ✅ D4. 本機重跑 `npm run build` 覆蓋舊 dist（2026-06-12 完成，JS 318KB / CSS 2.09KB）

## 建議起手順序

A1 → A3（拆完即有可測單元）→ A4 → A2+D2 → A5，其餘視進度插入。
