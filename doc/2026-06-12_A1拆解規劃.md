# 2026-06-12 A1 拆解規劃 — art_generate.py 業務下沉

**狀態：✅ 階段 1 已完成（2026-06-12，commit `e473bd1`，分支 a1_refactor，桌機先行動工）**

> ⚠ 實作早於本規劃合併，**模組命名與分組與下文不同**，以實際為準：
>
> | 本規劃 | 實際模組 | 差異 |
> |---|---|---|
> | Step 1a `image_utils.py` | `image_ops.py` | 不含 `_hex_to_sd_color`、`_detect_body_coverage`、`_create_inpaint_canvas_and_mask` |
> | Step 1b `workflow_io.py` | `workflow_builder.py` | 多含 `_inject_loras` |
> | Step 2 `wf_inject.py` | `wf_node_ops.py` | 不含 `_inject_loras`、`_inject_inpaint_nodes`、`_inject_img2img_node` |
> | Step 3 tag/vision 部分 | `vision_extract.py` | 含 `_detect_body_coverage`；主流程未下沉 |
>
> art_generate.py 3,050 → 1,856 行。**尚未搬**（= 後續 Step 3/4 範圍）：
> `_hex_to_sd_color`、`_create_inpaint_canvas_and_mask`、`_inject_inpaint_nodes`、
> `_inject_img2img_node`、`_run_canvas_expand_flux`、兩個主流程端點、request models（Step 4）。
> 另發現死碼 `_inject_txt2img`（無呼叫端，已搬至 wf_node_ops 並標註）。
**基準**：commit `26d32ad`，`backend/app/api/art_generate.py` **3,075 行**（13 endpoints + ~50 helpers）。
行號以該 commit 為準，**動工前先 grep 重新核對**。

## 目標

API 層只留路由與參數驗證（~600 行），業務邏輯下沉 `services/ai/` 四個新模組。

## 鐵則

1. **純搬移、零邏輯修改**——不改任何函式內容、不順手重構、不改行為
2. 保留函式原名（含 `_` 前綴），正名留待全部拆完後另開工作
3. import 方向單向：`api → services → models/schemas/core`；services **禁止** import api
4. 每 Step 一個獨立 commit（可單獨 revert）；commit message 寫明搬移清單
5. 拆解期間凍結 art_generate.py 的功能開發（避免衝突）

## Step 1a — `services/ai/image_utils.py`（純函式，無相依，最低風險）

| 函式 | 原行號 |
|---|---|
| `_border_color` | 1038 |
| `_letterbox_to_aspect` | 1047 |
| `_image_dimensions` | 1217 |
| `_clamp_dim` | 1241 |
| `_fullbody_canvas` | 1274 |
| `_is_flat_color_draft` | 1289 |
| `_hex_to_sd_color` | 1433 |
| `_detect_body_coverage` | 1460 |
| `_shrink_for_full_body` | 1571 |
| `_pixel_coverage_check` | 1605 |
| `_create_inpaint_canvas_and_mask` | 1824 |

## Step 1b — `services/ai/workflow_io.py`（workflow 載入/執行）

| 函式 | 原行號 | 備註 |
|---|---|---|
| `_resolve_style` | 73 | 吃 ArtStyle model，services 可 import models |
| `_compile_overrides` | 83 | |
| `_extra_tags` | 93 | |
| `_load_workflow` | 153 | |
| `_load_checkpoint_styles` | 206 | |
| `_detect_style` | 217 | |
| `_replace_negative_seeds` | 246 | |
| `_log_wf_snapshot` | 262 | |
| `_run` / `_run_comfyui` | 287 / 306 | |

## Step 2 — `services/ai/wf_inject.py`（節點注入/旁路，~900 行）

⚠ 互相呼叫密集，**整包一次搬，不可分批**。

| 群組 | 函式（原行號） |
|---|---|
| LoRA | `_inject_loras`(97) |
| IPA | `_wf_has_ipa`(637) `_find_ipa_loadimage_id`(645) `_inject_ipa_image`(683) `_bypass_ipa_nodes`(698) |
| CN | `_wf_has_controlnet`(762) `_find_main_ksampler_id`(787) `_inject_ipa_cn_nodes`(809) `_inject_controlnet_image`(912) `_bypass_controlnet_nodes`(957) |
| Prompt/組裝 | `_resolve_conditioning`(1071) `_inject_prompts`(1109) `_inject_txt2img`(1176) `_inject_controlnet_compose`(1192) |
| inpaint/i2i | `_inject_inpaint_nodes`(1872) `_inject_img2img_node`(1934) |

## Step 3 — `services/ai/character_design.py`（人設/變體主流程，風險最高）

| 群組 | 函式（原行號） |
|---|---|
| tag helpers | `_age_gender_tag`(1647) `_age_body_tags`(1666) `_height_body_tags`(1681) `_filter_visual_for_llm`(1708) `_visual_extract_prompt`(1729) |
| vision 快取 | `_vision_cache_key`(1769) `_vision_extract_cached`(1777) `_detect_coverage_and_extract_visual`(1493) |
| flux 擴圖 | `_run_canvas_expand_flux`(1991) |
| 主流程下沉 | `generate_character_design`(2037–2467) `generate_variant_design`(2471–2850) 主體 → service 函式；API 端點只剩參數驗證 + 呼叫 |

⚠ 兩主流程相似度 ~65% 但 IPA/CN 權重規則有分歧——**本步只搬不去重**，去重屬功能改進規劃 P7 另開。

## Step 4 — request models 移 `schemas/art_generate.py`

`CompilePromptRequest`(312) `GenerateRequest`(369) `GenerateAsyncRequest`(457)；
連動 `_build_txt2img`(379)、`_style_prompts`(531) 下沉（建議併入 workflow_io 或 wf_inject）。
此步解掉 services 對 api 內型別的最後依賴。

## 每步驗證門檻（全過才 commit）

1. `python -m compileall backend` exit 0
2. art_generate.py 內已搬函式定義 = 0（grep），改為 `from app.services.ai.xxx import ...`
3. 其他檔案若有引用舊位置（先 grep 全 repo），一併改 import
4. uvicorn 啟動 + `/docs` 開啟正常
5. 實機煙霧測試：txt2img 一張；Step 2 後加 IPA/CN 各一張；Step 3 後加人設生成一張

## 風險與對策

- **import 循環**：services 引用 api 內 model → Step 4 解；Step 1–3 期間暫以函式參數傳遞繞開
- **行號漂移**：每步搬完行號全變，下一步動工前重新 grep 定位
- **掛載寫入截斷**（06-10/06-12 實證）：搬移用 shell 寫入，寫完驗檔尾 + compileall
- **回退**：單 commit revert 即可，無 DB schema 變更

## 建議搭配

Step 1 完成後即插入優化規劃 A3 起頭：對 image_utils / workflow_io 補 pytest 單元測試（純函式最好測）。
